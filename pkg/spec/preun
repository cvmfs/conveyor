%preun

if [ "x$(systemctl list-unit-files | grep cvmfs-job-consumer)" != "x" ]; then
	systemctl stop cvmfs-job-consumer
fi

if [ "x$(systemctl list-unit-files | grep cvmfs-job-server)" != "x" ]; then
	systemctl stop cvmfs-job-server
fi
